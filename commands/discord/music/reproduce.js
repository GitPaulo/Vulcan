const reproduce     = module.exports;
const messageEmbeds = xrequire('./modules/messageEmbeds');

reproduce.execute = async (message) => {
    const musicManager = message.guild.musicManager;
    const request      = message.parsed.argsString;

};
